/**
 * Treatment catalog: named bundles of medications suggested for a given
 * condition. Two parallel views — human treatments and veterinary treatments —
 * are exposed through the same REST endpoints, filtered by the {@code
 * usageType} query parameter.
 */
package ma.pharmacie.treatment;

