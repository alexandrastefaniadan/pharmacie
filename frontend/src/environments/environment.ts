/**
 * Default environment (dev). Values are overridden in production by
 * file replacements configured in angular.json.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
};

